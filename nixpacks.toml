# Configuração do Nixpacks para Railway
# Evita problemas com pacotes apt

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "npm start"
